以后自己照着做：目录与命令
1. 工作目录（始终在项目根目录）
cd C:\Users\Administrator\Desktop\iam
所有 pnpm / nest 命令都在项目根目录执行，不要进 apps/iam 里跑。

2. 方式 A：用 Nest CLI 生成骨架（推荐）
# 进入项目根目录
cd C:\Users\Administrator\Desktop\iam
# 生成 service（指定子模块路径）
nest g service modules/application/application-setting/services/application-setting --project iam --no-spec
# 生成 controller
nest g controller modules/application/application-setting/controllers/application-setting --project iam --no-spec
# 生成 module（若还没有独立 module）
nest g module modules/application/application-setting --project iam
说明：

--project iam：对应 nest-cli.json 里的 iam 应用
--no-spec：不生成测试文件（与当前项目一致）
CLI 会创建文件，但路径/命名可能需手动微调以符合现有分层（dto/、entities/ 等）
3. 方式 B：完全手建（本次做法）
在对应子模块目录下手动建 4 类文件：

apps/iam/src/modules/<域>/<子模块>/
├── entities/*.entity.ts      # 已有则跳过
├── dto/create-*.dto.ts       # 入参校验
├── dto/update-*.dto.ts       # PartialType 继承 create
├── services/*.service.ts     # 业务逻辑 + TypeORM
├── controllers/*.controller.ts
└── *.module.ts               # forFeature + 注册 controller/service
然后在父模块里 import 子模块，例如 application.module.ts：

imports: [
  TypeOrmModule.forFeature([...]),
  ApplicationMenuModule,  // 新增这一行
],
4. 改完后的验证命令（仍在项目根目录）
cd C:\Users\Administrator\Desktop\iam
# 编译检查
pnpm build
# 开发启动（热更新）
pnpm start:dev
# 若改了 entity 字段，需要生成并执行迁移
pnpm migration:generate
pnpm migration:run
5. 快速对照：一个子模块最小 checklist
entities/ — 表结构（已有可跳过）
dto/ — class-validator 校验
services/ — @InjectRepository(Entity) + CRUD
controllers/ — @Controller('...') 映射 HTTP
xxx.module.ts — TypeOrmModule.forFeature + 注册 controller/service
父模块 imports: [XxxModule]
pnpm build 确认无报错
若你接下来要做 application-setting 或 application-role，结构可以完全照 application-menu 复制一份改名字即可。需要的话我可以直接帮你生成下一个。




# 数据库恢复命令

```
恢复数据（测试完菜单后执行）
方式一：直接覆盖恢复（推荐）

在 PowerShell 中执行：

Get-Content "c:\Users\Administrator\Desktop\iam\backups\iam-snapshot-20260702-111324.sql" | docker exec -i mysql8 mysql -u root -p123456 iam
方式二：先删库再恢复（更干净）

docker exec -i mysql8 mysql -u root -p123456 -e "DROP DATABASE IF EXISTS iam; CREATE DATABASE iam CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
Get-Content "c:\Users\Administrator\Desktop\iam\backups\iam-snapshot-20260702-111324.sql" | docker exec -i mysql8 mysql -u root -p123456 iam
恢复后建议：

浏览器清除 localStorage 里的 async-routes（或重新登录）
刷新页面，确认菜单恢复正常
以后再次做快照
$ts = Get-Date -Format "yyyyMMdd-HHmmss"
docker exec mysql8 mysqldump -u root -p123456 --single-transaction --routines --triggers --set-gtid-purged=OFF --default-character-set=utf8mb4 iam > "c:\Users\Administrator\Desktop\iam\backups\iam-snapshot-$ts.sql"
可以开始测试菜单的添加和删除了；需要恢复时执行上面的恢复命令即可。
```